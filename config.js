// Config padrão — deixe em branco aqui. O analista configura a URL e o
// token pela própria interface (botão ⚙), que salva no localStorage do
// navegador. Nunca coloque a URL/token direto neste arquivo se o
// repositório for público — eles ficam só no navegador de quem usa.
window.DEFAULT_CONFIG = {
  apiUrl: "",
  apiToken: ""
};
